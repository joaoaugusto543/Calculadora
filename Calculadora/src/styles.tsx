import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container:{
        flexGrow:1
    },
    buttons:{
        flexDirection:'row',
        flexWrap:'wrap'
    }
})

export default styles